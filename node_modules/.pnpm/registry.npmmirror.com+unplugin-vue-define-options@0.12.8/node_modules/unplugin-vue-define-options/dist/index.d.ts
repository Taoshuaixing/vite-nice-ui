import * as unplugin from 'unplugin';
import { FilterPattern } from '@rollup/pluginutils';

declare const transform: (code: string, id: string) => {
    code: string;
    map: any;
} | undefined;

interface Options {
    include?: FilterPattern;
    exclude?: FilterPattern;
}
type OptionsResolved = Omit<Required<Options>, 'exclude'> & {
    exclude?: FilterPattern;
};
declare const _default: unplugin.UnpluginInstance<Options | undefined, false>;

export { Options, OptionsResolved, _default as default, transform };
